# Gemini API Key (Optional)
# Get your free API key at: https://makersuite.google.com/app/apikey
#
# If set here, the app will use this key by default.
# The API key input field will only show if:
# 1. No key is set here AND no key is saved in browser
# 2. The key gets rate limited (429 error)
#
# Users can override this by entering their own key in the UI.
VITE_GEMINI_API_KEY=

# Spreuken App URL
# URL for the embedded Spreuken app iframe
VITE_SPREUKEN_APP_URL=http://localhost:3000

VITE_GEMINI_MODEL=gemini-2.5-flash